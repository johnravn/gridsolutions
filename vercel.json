{
  "crons": [
    { "path": "/api/cron/sync-conta", "schedule": "0 2 * * 0" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
